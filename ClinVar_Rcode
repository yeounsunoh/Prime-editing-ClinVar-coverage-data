rm(list=ls())

#########################

library("seqinr")

#library(BioMedR)

library(pssm)

library(seewave)

#library(msa)

library(stringr)

#library(openPrimeR)

library(primerTree)

library(rvest)

library(tidyverse)

library(Biostrings)



clinvar_total = read.csv("C://Users//tptt1//Downloads//clinvar_used_total.csv")

